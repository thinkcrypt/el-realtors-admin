import { Button } from '@chakra-ui/react';

const EditImageButton = () => {
	return (
		<Button
			position='absolute'
			top='0'
			right='0'
			size='xs'>
			Edit
		</Button>
	);
};

export default EditImageButton;
